import React from 'react';
import { View, Text } from 'react-native';

export const ImageSelectorScreen: React.FC = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Image Selector Screen</Text>
    </View>
  );
};
